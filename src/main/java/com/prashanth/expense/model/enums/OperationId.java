package com.prashanth.expense.model.enums;

public enum OperationId {
        SET_MONTH_CATEGORIES,
        SET_YEAR_CATEGORIES,
        SET_CREDIT_CATEGORIES,
        SET_EXPENSE_CATEGORIES,
        FILTER_BY_MONTH,
        FILTER_BY_YEAR,
        FILTER_BY_BILL,
        FILTER_BY_CARD,
        FILTER_BY_CARD_AND_EXPENSE,
        TOTAL_BALANCE,
        CARD_EXPENSE,
        DEFAULT_DATE,

}
